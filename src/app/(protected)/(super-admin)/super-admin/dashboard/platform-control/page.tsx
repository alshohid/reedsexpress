import RoleSectionPage from "@/src/components/design/RoleSectionPage";

export default function SuperAdminPlatformControlPage() {
  return (
    <RoleSectionPage
      eyebrow="Super Admin"
      title="Platform Control"
      description="Global platform controls now have a dedicated super-admin route instead of living inside the old admin tree."
    />
  );
}
