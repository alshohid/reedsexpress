import RoleSectionPage from "@/src/components/design/RoleSectionPage";

export default function SuperAdminManagementPage() {
  return (
    <RoleSectionPage
      eyebrow="Super Admin"
      title="Admin Management"
      description="Super admin ownership pages are now separated from the regular admin workspace and grouped under /super-admin/dashboard."
    />
  );
}
